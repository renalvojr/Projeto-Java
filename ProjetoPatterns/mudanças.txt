Extract method -> Funções retornarindice_projeto() e retornarindice_atividade(), linha 83; OK

Introduce Parameter Object -> You have a group of parameters that naturally go together, replace them with an object.
Your methods contain a repeating group of parameters. Replace these parameters with an object. 
-> criei a classe Dados para trocar esses parametros por um objeto

Extract Class -> Atividade e Projetos possuiam atributos em comum, para isso utilizei a classe Dados para resolver
(mostrar em Atividade e Projetos)

Chain constructor -> Utilizei o this(id, responsavel), para evitar duplicação de código.


Introduce Parameter Object -> Classe que guarda descrição, data de início e término de projetos e atividades, linha 144;
    falta mudar as paradas de quando eu for mudar eles (arrayDeAtividades.get(indice).dados.descricao = "Teste")
    falta fazer o mesmo para Projetos

